trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

variables:
  imageRepository: 'studenthub'
  dockerfilePath: 'Dockerfile'
  tag: '$(Build.BuildId)'

  # Service Connections
  acrServiceConnection: 'drconnection'
  azureSubscription: 'rgconnection'

  # Azure Resources
  containerRegistry: 'rojaacr.azurecr.io'
  aksResourceGroup: 'roja-rg'
  aksCluster: 'aks-studenthub'

stages:

# ============================
# Build Stage
# ============================

- stage: Build
  displayName: Build and Push Docker Image

  jobs:
  - job: DockerBuild
    steps:

    - checkout: self

    - task: Docker@2
      displayName: Build and Push Docker Image
      inputs:
        command: buildAndPush
        repository: $(imageRepository)
        dockerfile: $(dockerfilePath)
        containerRegistry: $(acrServiceConnection)
        tags: |
          $(tag)
          latest

# ============================
# Deploy Stage
# ============================

- stage: Deploy
  displayName: Deploy to AKS
  dependsOn: Build

  jobs:
  - job: AKSDeploy
    steps:

    - checkout: self

    - task: KubernetesManifest@1
      displayName: Deploy Application
      inputs:
        action: deploy
        connectionType: 'azureResourceManager'
        azureSubscriptionConnection: $(azureSubscription)
        azureResourceGroup: $(aksResourceGroup)
        kubernetesCluster: $(aksCluster)
        namespace: default

        manifests: |
          k8s/deployment.yaml
          k8s/service.yaml

        containers: |
          $(containerRegistry)/$(imageRepository):$(tag)
